h1 シフト希望

p
  | 来月のシフト希望を入力してください。
  br
  | 出勤できない日は出勤不可、出勤日に希望がある場合は出勤可にチェックを入れてください。
  br
  | 何も希望がない場合はプロフィール情報を元にシフトが決まります。

= form_with model: @request, url: requests_path, local: true do |f|
  = render 'shared/error_messages', object: f.object
  .form-group
    = f.label :date, '日付を選択してください'
    br
    = f.text_field :date, class: 'form-control', id: 'request_date', readonly: true
  .form-group
    = f.radio_button :possible, true, checked: true
    = f.label :possible, '出勤希望', value: true
    br
    = f.radio_button :possible, false
    = f.label :possible, '出勤不可', value: false
  br
  = f.submit '送信', class: 'btn btn-primary ml-4'
